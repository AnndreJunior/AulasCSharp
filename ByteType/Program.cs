// tipo primitivo que é utilizado para representar bytes (8 bits)
// muito útil quando usamos como um conjunto de bytes, como um arquivo, que é chamado de byte array
// temos também o sbyte, que permite valores negativos

sbyte cuida = -127;
// byte cuida = 127; // retorna erro